<h1>&lt;h2&gt;一、为何PDF转JPG如此普遍？&lt;/h2&gt;</h1>
<h2>二、方法一：pdf2image（Python脚本，适合大批量自动化）</h2>
<pre><code>pip install pdf2image
apt-get install poppler-utils   # Linux；macOS 用 brew install poppler</code></pre>

<b>核心代码示例</b>

<pre><code>from pdf2image import convert_from_path

images = convert_from_path('input.pdf', dpi=150)

for i, img in enumerate(images):
    img.save(f'page_{i+1}.jpg', 'JPEG')</code></pre>

只需几行代码就能将PDF的每一页保存成单独的JPG文件，文件名自动带上页码。

<b>关键参数说明</b>

<code>dpi</code>参数决定了输出分辨率，是影响图片品质的核心设置：

- <code>dpi=72</code>：适合网页预览，文件尺寸小
- <code>dpi=150</code>：日常办公的平衡点，清晰度足够且文件不过大
- <code>dpi=300</code>：需要打印或高质量归档时使用

还可以借助<code>first_page</code>和<code>last_page</code>参数指定只处理某几页，避免整本PDF都被转换：

<pre><code>images = convert_from_path('input.pdf', dpi=150, first_page=2, last_page=5)</pre></code>

<b>适用场景</b>

这种方法适合需要定期批量转换的自动化工作场景，比如每天从服务器拉取报告PDF并转成图片进行归档。对于偶尔转一两个文件的普通用户，配置环境的成本稍高，更推荐后面的两种方式。

<h2>三、方法二：PyMuPDF（fitz，速度快、控制精细）</h2>
<pre><code>pip install pymupdf</code></pre>

<b>使用方式</b>

<pre><code>import fitz  # pymupdf 导入时用 fitz

doc = fitz.open('input.pdf')

for i, page in enumerate(doc):
    mat = fitz.Matrix(2, 2)  # 2倍缩放，对应约 144 DPI
    pix = page.get_pixmap(matrix=mat)
    pix.save(f'page_{i+1}.jpg')</code></pre>

<code>fitz.Matrix(2, 2)</code>中的数字代表缩放倍数，调整这个值就能控制输出分辨率。想要更清晰，可以改为<code>fitz.Matrix(3, 3)</code>。

<b>进阶用法：只转特定页</b>

<pre><code>page = doc[0]   # 只取第一页，索引从0开始
pix = page.get_pixmap(matrix=fitz.Matrix(2, 2))
pix.save('first_page.jpg')</code></pre>

PyMuPDF还支持在导出前对页面做旋转、裁剪区域等操作，适合需要精细控制输出内容的场景，比如只截取PDF页面的某一块区域转成图片。

<b>适用场景</b>

PyMuPDF在处理速度上表现突出，大批量文件转换时效率明显。同时它对PDF格式的兼容性较好，遇到复杂排版或内嵌字体的PDF时，渲染结果通常也比较准确。适合对性能和灵活性都有要求的开发者使用。

<h2>四、方法三：命令行工具 pdftoppm（零代码，终端一行搞定）</h2>
<pre><code># Ubuntu / Debian
apt-get install poppler-utils

# macOS
brew install poppler</code></pre>

<b>基本用法</b>

<pre><code>pdftoppm -jpeg -r 150 input.pdf output_prefix</code></pre>

执行后会生成<code>output_prefix-1.jpg</code>、<code>output_prefix-2.jpg</code>……每页一个文件，自动编号。

<b>参数说明：</b>

- <code>-jpeg</code>：输出格式为JPEG（也支持<code>-png</code>输出PNG）
- <code>-r 150</code>：设置分辨率为150 DPI
- 最后两个参数分别是输入文件路径和输出文件名前缀

<b>只转某几页</b>

<pre><code>pdftoppm -jpeg -r 150 -f 3 -l 6 input.pdf output_prefix</code></pre>

<code>-f 3 -l 6</code>表示只处理第3到第6页，非常灵活。

<b>批量处理多个文件</b>

结合Shell循环，可以一次性批量处理整个目录下的所有PDF：

<pre><code>for f in *.pdf; do
  pdftoppm -jpeg -r 150 "$f" "${f%.pdf}"
done</code></pre>

<b>适用场景</b>

pdftoppm非常适合在服务器或自动化脚本中使用，不需要Python环境，调用开销低。对于运维、数据工程师来说，这是集成到Shell脚本或CI/CD流水线时的轻量方案。

<h2>五、手机上怎么把PDF转成JPG？微信小程序方案</h2>
<h2>六、在线网页工具：无需安装，浏览器直接操作</h2>
<h2>七、DPI怎么设置才合适？</h2>
<h2>八、批量转换PDF为JPG的注意事项</h2>
<h2>九、总结</h2><br>https://www.kmzp0871.com/articles/manual_manual_article_20260730_222645.html<br>